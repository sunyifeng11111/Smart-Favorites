{
  "name": "smart-favorites",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "build": "wxt build -b chrome",
    "compile": "tsc --noEmit",
    "dev": "wxt -b chrome",
    "evaluate": "node --import tsx scripts/run-evaluation.ts",
    "evaluate:gate": "node --import tsx scripts/run-evaluation.ts --require-beta-gate",
    "lint": "eslint .",
    "package": "wxt zip -b chrome && node --import tsx scripts/verify-package.ts",
    "postinstall": "wxt prepare",
    "pretest:browser": "pnpm build",
    "release:verify": "pnpm compile && pnpm lint && pnpm test && pnpm test:browser && pnpm package",
    "test": "vitest run",
    "test:browser": "playwright test",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@wxt-dev/module-react": "1.2.2",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "wxt": "0.21.4"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.63.0",
    "@types/chrome": "^0.3.0",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.1.1",
    "eslint": "10.11.0",
    "linkedom": "0.18.12",
    "tsx": "4.23.15",
    "typescript": "5.9.3",
    "typescript-eslint": "8.70.1",
    "vite": "8.3.0",
    "vitest": "5.0.1",
    "web-ext": "10.7.0"
  }
}
